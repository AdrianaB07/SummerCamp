@*@using SummerCamp.Models
@model IEnumerable<TeamViewModel>
@{
    ViewData["Title"] = "Teams";
}
<link rel="stylesheet" href="~/css/containerAlb.css" />
<h1 class="centered-title">Teams</h1>
<a asp-controller="Teams" asp-action="Add" class="btn btn-dark">Add team</a>
<div class="text-center">
    <div class="table-responsive container-white">
        <table class="table table-bordered table-condensed table-striped table-hover sortable">
            @if (Model.Count() == 0)
            {
                <tr>
                    <td colspan="6">No Records found.</td>
                </tr>
            }
            else
            {
                <thead class="thead-dark">
                    <tr>
                        <th>NickName</th>
                        <th>Name</th>
                        <th>Coach</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.NickName</td>
                            <td>@item.Name</td>
                            <td>@item.Coach?.FullName</td>
                            <td>
                                <a asp-action="Edit" asp-controller="Teams" asp-route-teamId="@item.Id" class="btn btn-dark">
                                    Edit
                                </a>
                                <a asp-action="MultiSelect" asp-controller="Teams" asp-route-teamId="@item.Id" class="btn btn-dark">
                                    SelectPlayers
                                </a>
                                <a asp-action="Delete" asp-controller="Teams" asp-route-teamId="@item.Id" class="btn btn-dark">
                                    Delete
                                </a>
                                        <a asp-action="Details" asp-controller="Teams" asp-route-teamId="@item.Id" class="btn btn-dark">
                                    Details
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            }
        </table>
    </div>
</div>
*@
@using SummerCamp.Models
@model IEnumerable<TeamViewModel>
@{
    ViewData["Title"] = "Teams";
}
<link rel="stylesheet" href="~/css/containerAlb.css" />
<h1 class="centered-title">Teams</h1>
<a asp-controller="Teams" asp-action="Add" class="btn btn-dark">Add team</a>
<div class="text-center">
    <div class="table-responsive container-white">
        <table class="table table-bordered table-condensed table-striped table-hover sortable">
            @if (Model.Count() == 0)
            {
                <tr>
                    <td colspan="6">No Records found.</td>
                </tr>
            }
            else
            {
                <thead class="thead-dark">
                    <tr>
                        <th>NickName</th>
                        <th>Name</th>
                        <th>Coach</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.NickName</td>
                            <td>@item.Name</td>
                            <td>@item.Coach?.FullName</td>
                            <td>
                                <a asp-action="Edit" asp-controller="Teams" asp-route-teamId="@item.Id" class="btn btn-dark">
                                    Edit
                                </a>
                                <a asp-action="MultiSelect" asp-controller="Teams" asp-route-teamId="@item.Id" class="btn btn-dark">
                                    SelectPlayers
                                </a>
                                <a asp-action="Details" asp-controller="Teams" asp-route-teamId="@item.Id" class="btn btn-dark">
                                    Details
                                </a>
                                <a asp-action="Delete" asp-controller="Teams" asp-route-teamId="@item.Id" class="btn btn-dark delete-button">
                                    Delete
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            }
        </table>
    </div>
</div>

<script>
    const deleteButtons = document.querySelectorAll('.delete-button');

    deleteButtons.forEach(button => {
        button.addEventListener('click', function (e) {
            e.preventDefault();

            const deleteUrl = button.getAttribute('href');

            if (confirm('Are you sure you want to delete this item?')) {
                window.location.href = deleteUrl;
            }
        });
    });
</script>
